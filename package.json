{
  "name": "uidepet-huellitassolidarias-api",
  "version": "1.0.0",
  "description": "API REST para UidePet - Huellitas Solidarias",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --exec tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:schema": "node --input-type=module -e \"import 'dotenv/config';import fs from 'fs';import mysql from 'mysql2/promise';const c=await mysql.createConnection({host:process.env.DB_HOST||'localhost',port:Number(process.env.DB_PORT||3306),user:process.env.DB_USER||'root',password:process.env.DB_PASSWORD||'',multipleStatements:true});await c.query(fs.readFileSync('db/schema.sql','utf8'));await c.end();console.log('Schema OK');\"",
    "seed": "node db/seed.mjs",
    "db:reset": "npm run db:schema && npm run seed"
  },
  "keywords": [
    "api",
    "node",
    "express",
    "uidepet",
    "huellitas-solidarias"
  ],
  "author": "Anyela Carpio",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.6",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^22.20.0",
    "nodemon": "^3.1.14",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3"
  }
}
